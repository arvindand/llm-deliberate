# OpenRouter API Configuration
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# LLM Configuration
# Timeout for API calls in seconds (default: 60)
LLM_TIMEOUT=60

# Maximum number of retries for failed API calls (default: 3)
LLM_MAX_RETRIES=3

# Default maximum rounds for multi-round deliberation (default: 3)
LLM_DEFAULT_MAX_ROUNDS=3

# Cost Warning Configuration
# Warn user if estimated cost exceeds this threshold in USD (default: 5.0)
COST_WARNING_THRESHOLD=5.0
